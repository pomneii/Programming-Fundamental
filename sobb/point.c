
#include <stdio.h>

int main() {

    // int x1, y1;
    // scanf("%d %d", &x1, &y1);
    // int x2, y2;
    // scanf("%d %d", &x2, &y2);
    // int x3, y3;
    // scanf("%d %d", &x3, &y3);
    // int x4, y4;
    // scanf("%d %d", &x4, &y4);
    // int x5, y5;
    // scanf("%d %d", &x5, &y5);

    // int maxX = x1 - x2;
    // int maxY = y1 - y2;
    // if((x2 - x3) > maxX && (y2 - y3) > maxY) {
    //     maxX = x2 - x3;
    //     maxY = y2 - y3;
    // }
    // if((x3 - x4) > maxX && (y3 - y4) > maxY) {
    //     maxX = x3 - x4;
    //     maxY = y3 - y4;
    // }

    // if((x4 - x5) > maxX && (y4 - y5) > maxY) {
    //     maxX = x4 - x5;
    //     maxY = y4 - y5;
    // }

    // printf("")
    

    //printf("%d %d", x[2], y[2]);

    int num[10], x[5], y[5];
    for(int i = 0 ; i < 5 ; i++) {
        scanf("%d %d", &x[i], &y[i]);
    }

    return 0;
}